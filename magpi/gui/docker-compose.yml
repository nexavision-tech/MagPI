services:
  magpi-vite:
    image: node:18-alpine
    container_name: magpi-vite-dev
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host"
